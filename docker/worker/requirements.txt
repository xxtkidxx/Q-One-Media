# Worker — pin cứng để build lặp lại được.
# Torch/torchaudio cài riêng trong Dockerfile (index cu124).

# --- Thu nạp ---
yt-dlp==2026.8.19

# --- ASR + forced alignment ---
# WhisperX: timestamp cấp từ ±50ms (vs ±500ms Whisper thuần).
# Dùng cho 2 việc: transcript nguồn, và căn phụ đề Việt theo audio TTS.
whisperx==3.3.1
faster-whisper==1.1.0
# ctranslate2 >= 4.5 la BAT BUOC, khong phai chon ban moi cho vui.
# Ban 4.4.0 link voi cuDNN 8, con base image CUDA 12.4 va torch 2.6 deu mang
# cuDNN 9 - khong co cuDNN 8 o dau. Hau qua: CTranslate2 khong nap duoc
# libcudnn_ops_infer.so.8 va ABORT cung ca tien trinh (Fatal Python error:
# Aborted), khong phai nem exception. Nghia la duong ASR tren GPU khong chay duoc
# mot lan nao. faster-whisper 1.1.0 cho phep ctranslate2 <5 nen 4.8.x hop le.
ctranslate2==4.8.2

# --- Tách giọng khỏi tiếng máy ---
# Bỏ stem giọng gốc, GIỮ stem tiếng máy làm nền (mục F2.4).
demucs==4.0.1

# --- TTS ---
# VoxCPM2: Apache-2.0, có tiếng Việt, voice cloning + voice design.
# Cài từ PyPI nếu có; nếu không thì vendor theo scripts/fetch_models.py.
# voxcpm 0.5.0 KHONG TON TAI - so do la lan ten model (VoxCPM-0.5B) thanh so
# phien ban package. Ban that len toi 2.0.3.
voxcpm==2.0.3

# --- Xử lý media ---
ffmpeg-python==0.2.0
soundfile==0.12.1
numpy==1.26.4

# --- Hạ tầng ---
psycopg[binary]==3.2.3
sqlalchemy==2.0.36
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
httpx==0.28.1
tenacity==9.0.0
structlog==24.4.0

# --- LLM ---
anthropic==0.42.0

# TTS du phong cho DEV: chay duoc toan chuoi khong can GPU.
# BI CHAN o production - xem src/infrastructure/tts/edge.py
# edge-tts KHONG pin cung mot phien ban cu duoc: no la client khong chinh thuc,
# phai chay theo thay doi handshake cua Microsoft. Ban 7.0.2 da bi 403
# (WSServerHandshakeError) vao 14/09/2026 trong khi 7.2.8 chay duoc.
# Day chinh la ly do ky thuat de KHONG dung no o production.
edge-tts>=7.2.8
